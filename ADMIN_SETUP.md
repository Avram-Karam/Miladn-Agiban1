# إعداد نظام الدرجات ولوحة الأدمن — ميلادا عجيبًا 2026

## 1) Google Apps Script

استبدل محتوى `Code.gs` في Apps Script بالملف:

`assets/js/google-apps-script-Code.gs.js`

ثم اعمل **Deploy > Manage deployments > Edit > New version > Deploy**.

السكربت ينشئ تلقائيًا:
- `GameScores`
- `IndividualScores`
- `SiteConfig`

ولا يحذف بيانات `Attendees` القديمة.

## 2) Vercel

ارفع المشروع ثم انتظر الـ deployment.

يمكنك تخصيص بيانات الأدمن من Environment Variables:
- `ADMIN_USERNAME` = `admin`
- `ADMIN_PASSWORD` = `124578`
- `ADMIN_SESSION_SECRET` = مفتاح طويل عشوائي
- `GAS_URL` = رابط نشر Apps Script
- `GAS_TOKEN` = `YC2026_SECURE_TOKEN_8921`

إذا لم تضفها، النسخة الجديدة تحتوي على قيم افتراضية للمؤتمر حتى تعمل مباشرة، لكن يفضل ضبطها في Vercel.

## 3) لوحة الأدمن

الرابط:

`/admin.html`

بيانات الدخول الافتراضية:
- Username: `admin`
- Password: `124578`

من اللوحة يمكن:
- تعديل درجات الألعاب الخمس.
- تعديل درجات المحاضرتين.
- تعديل درجات حضور والتزام الثمانية.
- تعديل درجة البانفلت لكل فرد من 0 إلى 50.
- تعديل أسماء المجموعات وأعضائها.
- تعديل الغرف والأشخاص داخلها.
- مشاهدة إجمالي كل مجموعة من 300.

## 4) حساب الـ300

- ترنيمة وتقديمها: 15
- تحت الضغط: 15
- اسكتش مسرحي: 30
- استوديو تحليلي بمود شخصية: 20
- تسميع اللحن الخاص بالمؤتمر: 20
- المحاضرة الأولى: 15
- المحاضرة الثانية: 15
- حضور والتزام: 120
- البانفلت: 50

الإجمالي = **300 درجة**.

درجات الألعاب للمجموعة مباشرة، بينما درجات المحاضرات والحضور والبانفلت فردية ويتم حساب متوسط أفراد المجموعة ثم إضافته إلى رصيد المجموعة.

## 5) مهم بعد تحديث النسخة

بعد رفع النسخة على Vercel، يجب **نشر نسخة جديدة من Google Apps Script** باستخدام الملف المحدث أعلاه. إصلاح `saveGameAttempt` وقراءة سجل الدرجات وإعادة فتح كويز المحاضرات يعتمد على أن نسخة Web App المنشورة من Apps Script تحتوي على الكود المحدث.

### ما تم إصلاحه
- قراءة `getScorebook` أصبحت تعمل من مسار القراءة الصحيح بدل إرسالها كعملية POST عادية.
- `saveGameAttempt` موجود ومُسجل في `doPost` لحفظ محاولات الألعاب مرة واحدة.
- الطالب لا يستطيع إعادة تسجيل درجة المحاضرة بعد تسجيلها؛ المحاولة الثانية ترجع `already_submitted`.
- زر **إعادة المحاولة** أُزيل من كويز المحاضرات.
- لوحة الأدمن أصبحت تستخدم بوابة الأدمن المصادق عليها لقراءة سجل الدرجات.
- أضيف في الأدمن تبويب **فتح الكويز** لإعادة فتح كويز المحاضرة الأولى أو الثانية لطالب محدد ومجموعة محددة.
- إعادة الفتح تتم بحذف سجل درجة الكويز فقط، وبعدها يستطيع الطالب حل الكويز مرة أخرى.
- الحضور والبانفلت يظلان قابلين للتعديل من صفحات التسجيل المخصصة، لأنهما ليسا محاولات كويز للطالب.


## ملاحظات الإصلاحات الحالية
- `saveGameAttempt` وعمليات الدرجات موجودة في نسخة Google Apps Script المرفقة بالمشروع؛ بعد رفع المشروع على Vercel يجب تحديث Deployment الخاص بـ Apps Script إلى هذه النسخة.
- كويز المحاضرة يُسجل مرة واحدة للطالب/المجموعة/المحاضرة. فتح الكويز بالخطأ بدون تسجيل درجة لا يمنع الطالب من المحاولة؛ بعد تسجيل الدرجة فقط تُقفل المحاولة.
- إعادة فتح محاولة مسجلة تتم من لوحة الأدمن فقط من تبويب `🔓 فتح الكويز`.
- لا تغيّر Environment Variables الموجودة في Vercel إلا إذا كنت تعرف قيمها الحالية؛ خصوصًا `GAS_URL`, `GAS_TOKEN`, `ADMIN_USERNAME`, `ADMIN_PASSWORD`, `ADMIN_SESSION_SECRET`.
